import Login from "../login/login";
import Header from "../header/Header";

/* imported the Login and header components for demonstration purposes only. I still have a ways to go with developing the nav functionality. Ideally the nav will only be visible once the user is authenticated and logged in with an active session. */

export default function Homepage() {
  return (
    <div>
        <h1>Please Log in</h1>
    </div>
  );
}
